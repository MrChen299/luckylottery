{
  "name": "luckylottery-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute lottery-db --file=./db/schema.sql",
    "db:local": "wrangler d1 execute lottery-db --file=./db/schema.sql --local"
  },
  "dependencies": {
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250607.0",
    "wrangler": "^4.13.0"
  }
}